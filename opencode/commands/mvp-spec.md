---
description: Research and produce a strict MVP spec with small 1-2 hour tasks and explicit out of scope.
agent: scoper
---


Create a strict MVP spec for:

$ARGUMENTS

Use the `/scope` flow only: `researcher -> scoper synthesis -> specifier`.

Additional constraints:

- Keep the MVP minimal.
- Prefer tasks sized for 1-2 hours.
- Make out of scope explicit.
- Avoid speculative platform, architecture, or polish work.
- Include acceptance criteria and validation for every task.
- Stop if research is not ready for spec.
